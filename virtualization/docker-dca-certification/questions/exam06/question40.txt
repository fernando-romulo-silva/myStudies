Which of the following commands will provide a list of all Docker networks and their 
drivers currently configured on the Docker host? Choose the correct answer:

a) docker system --list-networks

b) docker network ls

c) docker info --networks
​
d) docker system inspect networks












































'B'

docker network ls: Lists all the networks the Engine `daemon` knows about. 

This includes the networks that span across multiple hosts in a cluster.

$ docker network ls
