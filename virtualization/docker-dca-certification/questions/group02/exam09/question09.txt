Which of the following isolation technologies is used in Docker Desktop for Windows?

A)
namespace

B)
hypervisor

C)
cgroups

D)
users 





































Answer B

On Microsoft Windows, you can specify these values:

hyperv:	Hyper-V hypervisor partition-based isolation.

process: Namespace isolation only.

default: Use the value specified by the Docker daemon’s --exec-opt . 
         If the daemon does not specify an isolation technology, Microsoft Windows uses process as its default value.