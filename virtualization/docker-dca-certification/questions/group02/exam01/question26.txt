You have received a docker service inspect report that appears to be formatted as one big json. 
What option can you use in order to clean up the format into something more readable?

A)
 --human
 
B)
--pretty

C)
--readable

D)
--format="{?{.Structure.To.Review}}"




































Answer B

You can print the inspect output in a human-readable format instead of the default JSON output, by using the –pretty option.