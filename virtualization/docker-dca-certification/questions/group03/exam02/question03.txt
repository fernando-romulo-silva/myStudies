Which of the following options below can be used for scaling the nginx service on a Docker Swarm?

A)
docker swarm scale nginx=3

B)
docker service scale nginx=3

C)
docker service --replicas=3 nginx

D)
docker swarm --replicas=3 nginx





































Answer B

The command 'scale' Scale one or multiple replicated services.

$ docker service scale frontend=50

You can also scale a service using the docker service update command. 
The following commands are equivalent:

$ docker service update --replicas=50 frontend