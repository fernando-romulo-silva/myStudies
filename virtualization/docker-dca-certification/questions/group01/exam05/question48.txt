Which command is used to join a swarm as a worker?

a) docker swarm -j
​
b) docker join the swarm
​
c) docker worker init
​
d) docker swarm join














































Answer 'D'

Join a swarm as a node and/or manager

$ docker swarm join

