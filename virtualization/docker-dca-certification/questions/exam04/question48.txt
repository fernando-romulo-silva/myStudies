Which command can you use to clean up multiple types of objects (images, containers, and networks) at once?

​A)
docker system purge
​
B)
docker cleanup
​
C)
docker clean all
​
D)
docker system prune







































D is correct

Remove all unused containers, networks, images (both dangling and unreferenced), and optionally, volumes.

$ docker system prune 

