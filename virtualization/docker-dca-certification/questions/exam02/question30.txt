After creating a new service named 'http', you notice that the new service is not registering as healthy. 
How do you view the list of historical tasks for that service by using the command line?

A)
docker service inspect http'
​
B)
'docker service ps http'
​
C)
'docker inspect http'
​
D)
'docker ps http'




































The correct answer is A

Display detailed information on one or more services.

& Find the number of tasks running as part of a service &

The --format option can be used to obtain specific information about a service. 

For example, the following command outputs the number of replicas of the “redis” service:

$ docker service inspect --format='{{.Spec.Mode.Replicated.Replicas}}' redis

